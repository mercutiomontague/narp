require 'narp/basic_g.treetop'
require 'narp/include_extensions.rb'


module Narp
  grammar IncludeG 
    include BasicG
  
  	rule include 
  		'/INCLUDE'i space condition_name <Include>
  	end
  
  	# rule condition_name 
    #   ('ALL'i <MyIdentifier>) / (identifier &{|o| myapp.conditions.detect{|f| f.name == o.first.value } } <MyIdentifier>)   
    # end
  
  end
end
